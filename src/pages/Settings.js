import settingsImage from '../assets/settings.png';

function Settings() {
  return (
    <div className="page">
      <img src={settingsImage} alt="Settings" className="page-image" />
    </div>
  );
}

export default Settings;
